//AreaOfRectangle in c 
#include<stdio.h>
int main()
{
  float l,w,a;
  printf("enter length:");
  scanf("%f",&l);
  printf("enter width:");
  scanf("%f",&w);
  a=l*w;
  printf("Area of rectangle is :%f",a);
  return 0;
}
